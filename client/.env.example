# No backend needed for the client-only deployment
# The app calls bible-api.com and Anthropic API directly from the browser
# (Anthropic API calls are handled via Claude Artifacts' built-in proxy)
VITE_API_URL=http://localhost:5000
